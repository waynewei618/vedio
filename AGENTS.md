# 项目级 Codex 指令

## Python 环境

- 本项目如果需要用 Python 执行代码，使用 conda 的 `veddo` 环境。
- 运行 Python 命令前先激活环境：`conda activate veddo`。
- 例外：本地中文 TTS/自动配音流水线使用 conda 的 `veddo-tts` 环境，直接运行：
  `/home/sil/workspace/conda_envs/veddo-tts/bin/python`。
